### Concurrency
<p align="center"><em>Make it work, make it right, make it fast</em></p

- Non-blocking operations run in a separate process "goroutine"
- Channels are data structure than can receive and send values
  - useful when communicating between multiple processes
